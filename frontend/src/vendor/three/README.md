# Vendored Three.js (MIT) for Willy.

| Path | Role |
|------|------|
| `LICENSE` | MIT license |
| `three.core.min.js` | Core (imported by the module build) |
| `three.module.min.js` | ES module — Vite alias `three` |

Kept under `src/vendor` so Vite can bundle it. Do not import Three from `public/`.
