# Base URL of the API. Leave unset in development: Vite proxies /api to the
# backend so the browser stays on one origin.
# VITE_API_BASE_URL=https://api.yourdomain.com/api/v1

# Override the dev proxy target if the API is not on 127.0.0.1:8000.
# VITE_PROXY_TARGET=http://127.0.0.1:8000
